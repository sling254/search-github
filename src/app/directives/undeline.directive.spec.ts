import { UndelineDirective } from './undeline.directive';

describe('UndelineDirective', () => {
  it('should create an instance', () => {
    const directive = new UndelineDirective();
    expect(directive).toBeTruthy();
  });
});
